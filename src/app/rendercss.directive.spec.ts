import { RendercssDirective } from './rendercss.directive';

describe('RendercssDirective', () => {
  it('should create an instance', () => {
    const directive = new RendercssDirective();
    expect(directive).toBeTruthy();
  });
});
